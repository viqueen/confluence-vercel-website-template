{
  "name": "@labset/confluence-vercel-website-template",
  "version": "1.0.0",
  "scripts": {
    "site:env": "confluence-website init-env",
    "site:init": "confluence-website init-site",
    "site:extract": "confluence-website extract-space website",
    "site:build": "confluence-website build-space website --assets ./assets"
  },
  "devDependencies": {
    "@labset/confluence-website": "^0.0.6"
  }
}
