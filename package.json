{
  "name": "@labset/confluence-vercel-website-template",
  "version": "1.0.0",
  "scripts": {
    "site:env": "confluence-website init-env",
    "site:init": "confluence-website init-site",
    "site:extract": "confluence-website extract-site ${CONFLUENCE_SPACE_KEY}",
    "site:build": "confluence-website build-site ${CONFLUENCE_SPACE_KEY} --assets ./assets"
  },
  "devDependencies": {
    "@labset/confluence-website": "^0.1.1"
  }
}
